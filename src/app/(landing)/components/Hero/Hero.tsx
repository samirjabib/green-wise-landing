import { HeroBackground } from "./HeroBackground";

export const Hero = () => {
  return (
    <div className="" id="hero">
      <HeroBackground />
      <div>
        <h1>Card Float</h1>
        <p>Text Scrolll</p>
        <button>submit</button>
      </div>
    </div>
  );
};
